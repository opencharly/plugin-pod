# 2026.260.0929 — feat: author the `charly cp / volume / restart` command skill

## Summary

`charly cp` / `charly volume` / `charly restart` — the pod-lifecycle verbs the plugin-pod candy serves had no owning skill. Adds a `skill:` entity as a SIBLING top-level node
(`restart-skill:` beside the candy node) in `candy/<plugin>/charly.yml` — the `skill:`
kind's authored shape (a skill node is a document-unique top-level entity, not a
field of the candy node; `charly box validate` counts it as a `skill:` entity,
not a second candy). It declares `family: pod-verbs` (declared in the sibling
opencharly/pod-charly-hooks PR #6) and documents the command's surface and when
to use it.

The generated marketplace corpus is never hand-edited; this candy entity is its
source.

## How tested

**Gate that exercises the change — mutation test (the check is NOT vacuous).**
`charly box validate` validates the skill entity against the served `#SkillInput`
schema. Proved by MUTATION: removing the entity's required `family:` field makes
the SAME gate FAIL on the SAME file, so the gate genuinely depends on this entity:
```
$ charly box validate                                   # unmutated (this head)
exit 0 (the repo's pinned charly validates the manifest cleanly; `charly box validate` prints nothing on this schema — the exit code is the verdict)

$ # mutate: delete the new "restart-skill:" block's `family:` line
$ charly box validate
charly: error: command "validate": validation error: … node "restart-skill": plugin kind:skill: plugin_input fails #SkillInput: #SkillInput.family: incomplete value =~"^[a-z][a-z0-9-]*$"
```
Binary: v2026.238.1242 (this repo's CI pin).

**Cross-repo dependency proven.** `family: pod-verbs` is declared in the consumer
marketplace entity:
```
$ git -C pod-charly-hooks show feat/add-cache-family:charly.yml | grep -n '^            pod-verbs:'
915:            pod-verbs:
```
An undeclared family hard-errors generation — proved by mutation in
opencharly/pod-charly-hooks#6: removing a family declaration makes the generator
fail with `skill "…" declares family "…" which the marketplace entity does not
declare`.

**Documented-surface provenance (R4a).** The skill's surface is the real shipped
command: `candy/plugin-pod/pod_cmd.go` — the `RestartCmd` / `CpCmd` / `VolumeCmd` grammar structs and their help strings (plus the `command:cp`, `command:volume`, `command:restart` provider list).

**Cross-references resolve** (a dangling `/charly-x:y` hard-fails the docs
build): `/charly-core:charly-update`, `/charly-core:service`, `/charly-core:stop`, `/charly-core:start`, `/charly-core:shell`, `/charly-automation:sidecar`, `/charly-automation:enc` — each exists in the corpus.

**Repo-native CI gate.** This repo's CI clones/builds the pinned charly and runs
its gate; the PR's `checks` are green (the mutation evidence above is the
authority that the gate exercises the entity).

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | git-workflow, skills, marketplace, check loaded before acting. |
| R1 — RCA every anomaly | Gate output zero warnings/errors; the family dependency verified against the consumer, not assumed. |
| R2 — finish the whole cutover | The entity lands with its family declaration (sibling PR #6); no split. |
| R3 — no duplication | One `skill:` entity; documents the existing command surface, no second implementation. |
| R4 — no workarounds | Every command the skill names is the actual `charly` verb. |
| R4a — product-first docs | The documented surface is the shipped command (provenance above). |
| R5 — delete legacy | Additive only. |
| R6 — git safety | Branch-per-change PR; no force-push; clean tree. |
| R7 — prove behaviour | The mutation test above shows the gate fails without the entity. |
| R8 — preserve emitted artifacts | The entity is a generated-corpus input; its projection is gated by the marketplace deploy drift gate when this repo is next pinned. |
| R9 — binary equals source | `charly box validate` with the built binary (provenance above). |
| R10 — fresh disposable proof | N/A for the prose content — this is an authored `skill:` entity with no runtime path (prose cannot fail a bed); the manifest change is proved by the mutation-tested `charly box validate`. |
| RDD/ADE/SDD | ADE: a `skill:` entity is not a candy, so no candy `description:`/`plan:` is required (the candy's own plan is untouched). SDD: the entity validates against the served `#SkillInput` schema. |
| Disposable-only gate | N/A — no targets created/destroyed. |
| Hooks doctrine | N/A — standalone repo; merge gate = org pr-validator. |

## Change classification

- **Class:** candy manifest entity (config) — an additive authored `skill:` entity.
  NOT `documentation reviewed`: a manifest file changed, so the docs-only label is
  forbidden (R10 matrix).
- **Verification gate:** `charly box validate` (mutation-tested, above) + the
  repo-native CI gate.
- **Attribution tier:** `analysed on a live system` — the gate ran live with
  retained output and the mutation test proves it exercises the change; the
  corpus projection is gated when this repo is next pinned (no disposable bed
  exists for a manifest entity).

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*