# 2026.250.0546 — feat!: retire the fleet vocabulary — spec/fleet → spec/deploy (CC-2 consumer)

## Summary

Cutover C task 2 (the fleet-vocabulary retirement) — the plugin-pod consumer PR of the spec IR rename (spec #111 v0.2026250.523 + sdk #229 v0.2026250.527). The fleet vocabulary is RETIRED (operator ruling: usage frequency is never an argument to keep unclean code).

**Re-pin**: sdk v0.2026234.347 → v0.2026250.527, spec v0.2026232.520 → v0.2026250.523.

**Renames**:
- import `github.com/opencharly/spec/fleet` → `github.com/opencharly/spec/deploy`
- `fleet.*` package qualifiers → `deploy.*`
- `charly fleet add/del/…` → `charly deploy add/del/…` in comments

**R1 fix**: `pod_update_test.go` used the pre-CC-1 `Children` map (the member-tree rebuild replaced Children/Members with the uniform Member tree) — the test now builds the Member tree with stamped positions.

**Claim-keyed sweep** (`git grep -iw fleet`, non-CHANGELOG): ZERO vocabulary matches. The sole remaining matches are references to the plugin-fleet REPO (which keeps its name — repo renames are org-level, outside PR scope).

## How tested

**Eval beds run: N/A — no disposable bed can execute this diff yet.** A live `charly check run` of a bed requires the charly binary to pin the NEW plugin tags; the charly consumer re-pin (the in-flight wave item, opencharly/charly CC-2) is the landing that carries the wave's bed proof — a legitimate self-blocking-proof situation (rule 3), named here per the plugin-check #29 precedent. Compile blast radius: the previous sdk pin (v0.2026234.347) cannot build against spec v0.2026250.523 (the `spec/fleet` package is deleted), so no bed running a pre-523 charly can load this plugin build.

**Did the CHANGED code path execute?** The changed code compiled and its unit-level behavior executed in the Go test environment: the full `go test -count=1 ./...` suite exercises the renamed `deploy.*` qualifier paths (all packages ok, output below). The changed branch did NOT run live — the tier below reflects that.

```
$ go get github.com/opencharly/spec@v0.2026250.523 github.com/opencharly/sdk@v0.2026250.527
go: upgraded github.com/opencharly/sdk v0.2026234.347 => v0.2026250.527
go: upgraded github.com/opencharly/spec v0.2026232.520 => v0.2026250.523

$ go build ./... && go vet ./... && gofmt -l .   # all clean (exit 0)
$ go test -count=1 ./...   # all packages ok

$ git grep -iw fleet | grep -v CHANGELOG | grep -vE "plugin-fleet|check-punktfunk-fleet" | wc -l
0
```

## Rulebook compliance

- **R0**: git-workflow + strict-policy skills loaded before the first tool call.
- **R1**: `pod_update_test.go` `Children` field failure RCA'd — a CC-1 member-tree residual; fixed to the Member tree shape.
- **R3**: one canonical name per concept — `DeployNode`/the `deploy:` map.
- **R4**: no workarounds — the rename is complete, not aliased.
- **R5**: the old identifiers are DELETED, not aliased.
- **R6**: PR-only landing, no force-push, no direct main push.
- **R7**: gates proven above (build/vet/test/gofmt + the identifier sweep).
- **R8/R9**: N/A — no generation/Containerfile/binary change in this repo.
- **R10**: the bed gate rides the charly consumer re-pin (named + reasoned above); the unit-level coverage half is satisfied by the full test suite.

## Change classification

- **Change class**: breaking consumer rename (compiled-in plugin).
- **Verification gate**: `go build`/`go vet`/`go test`/`gofmt` all green on the final committed tree; the changed branch did NOT run live (the wave bed proof rides the charly consumer re-pin).
- **Attribution tier**: analysed on a live system — the changed runtime path (the `deploy.*` qualifier reads) is exercised by the unit suite; the live bed proof rides the charly consumer re-pin (self-blocking-proof, rule 3).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*