# 2026.266.1109 — feat(pod): read the deploy engine through the EngineName type

## Summary

Consumer leg of the **engine provider class** cutover (spec#144/#145, sdk#272 — both merged). The spec typed every authored engine field as `#EngineName` (`spec.EngineName`). `plugin-pod`'s per-image engine resolution in the remove path is the consumer: it read the deploy entry's `Engine` straight into a string.

- `runEngine = string(entry.Engine)` — an explicit conversion at the one read site, matching how the sdk's own `deploykit/box_engine.go` / `deploy_file.go` now read the same field
- spec pin `v0.2026250.523` → `v0.2026266.545`; sdk `v0.2026250.527` → `v0.2026266.623`. The `go.sum` also completes the standalone (workspace-masked) transitive entries (`go-isatty` go.mod hash + the relocated `plugin-egress`/`plugin-init` requires).

## How tested

**Change-class gate:** `charly check run check-boxload-pod` (kind:pod, `disposable: true`), branch-built engine-cutover charly `2026.266.0826`, `plugin-deploy-pod` composed via `CHARLY_REPO_OVERRIDE`.

**Disposable target:** `check-boxload-pod` carries `disposable: true` (kind:pod); its `cleanup` step IS `charly remove`, so the fully-ported `remove_orchestration.go` path executes live.

```
$ charly check run check-boxload-pod
[config] PASS 32.443s  [start] PASS 25.826s  [check-live] PASS 1m43.056s
[update] PASS 2m13.748s  [check-live-rebuild] PASS 1m38.688s
[cleanup] PASS 29.628s          # ← charly remove → remove_orchestration.go, the changed file
PASS (steps=14)
```
`.check/check-boxload-pod/2026.266.0941/summary.yml` → `ok: true`, 0 failed steps.

**Fresh rebuild:** `[update] PASS 2m13.748s` + `[check-live-rebuild]`.

**Changed-path execution:** the remove orchestration ran live. The one converted line, `runEngine = string(entry.Engine)`, sits inside `if … entry.Engine != ""` and this bed's deploy authors no `engine:`, so that branch did not fire (the conversion is compile-enforced against the typed field). Per A3 the honest tier is therefore `analysed on a live system` — see the footer.

Local gates (standalone, `GOWORK=off`): `gofmt -l .` (empty) · `go build ./...` · `go vet ./...` · `go test ./...` (`ok … 0.627s`).

**R5 sweep (executed):**
```
$ grep -rn "JumpPodmanExec\|JumpDockerExec" --include='*.go' candy/plugin-pod
(no matches)
```

## Project-rulebook rule-compliance

- **R0** — `/charly-check:check`, `/charly-internals:plugin`, `/charly-internals:git-workflow` loaded first.
- **R1** — the workspace-masked `go.sum` defect was root-caused and fixed in-tree.
- **R2** — the sibling wave legs are named on opencharly/charly#633.
- **R3** — one `EngineName` read idiom, matching the sdk.
- **R4** — no shim.
- **R5** — no stale identifier (sweep above empty).
- **R6/R7** — green build/vet/test + the real bed.
- **R8** — no emitted artifact changed.
- **R9** — the bed runs the branch binary `2026.266.0826`.
- **R10** — the disposable fresh-rebuild cycle above.
- **RDD** — the pod-lifecycle paths (config/start/check/remove) ran live.
- **ADE** — the candy's baked plan ran.
- **Disposable-only autonomy** — only the bed's own `disposable: true` teardown.

## Change classification

Plugin Go code on a runtime path (per-image engine resolution) + dependency pin bump. Verification gate: `check-boxload-pod` disposable R10 bed (fresh rebuild) + standalone Go gates. Attribution tier: `analysed on a live system` (the remove path ran live; the one non-default `#EngineName` branch did not fire in this bed).

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*